<div class="container">
 <h4>新着商品</h4>
  <div class="row">
    <div class="col-md-8 offset-md-1">
        <% @items.each do |item| %>
        <%= image_tag post_image.get_image %>
         <% item.name %>
         <% item.price %>
        <% end %>
    </div>
  </div>
</div