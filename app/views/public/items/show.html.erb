<div class="container">
  <div class="row">
    <div class="col-md-8 offset-md-1">
      <%= image_tag @item.image, size: "200x200" %>
      <span><%= @item.name %></span>
      <h5><%= @item.introduction %></h5>
      ¥<%= @item.price %><h6>(税込)</h6>
       
    </div>
  </div>
</div